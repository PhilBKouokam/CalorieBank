// Default presentation retains the canonical iOS wording.
export const notificationSettingsName = 'iOS Settings';
export const deletionHealthNote = ' You can manage Apple Health permissions separately in iOS.';
export const nativeRefreshFailure = 'Apple Health couldn’t refresh. Try again.';
export function sourceLabel(label: string) { return label; }

# Mobile App Notes

This workspace uses Expo SDK 57 with Expo Router under `src/app`.

Follow the root `AGENTS.md` rules. Keep mobile changes scoped to the current vertical slice and do not add business features before the shared domain and API contracts are ready.
